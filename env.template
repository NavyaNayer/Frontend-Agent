# Environment Configuration Template
# Copy this file to .env and fill in your actual values

# OpenAI API Key (required for component generation)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Target URL to crawl (the website you want to replicate)
TARGET_URL=https://app.asana.com

# Authentication Credentials (if the site requires login)
# For Asana example:
ASANA_EMAIL=your_email@example.com
ASANA_PASSWORD=your_password_here

# CSS Selectors to mask dynamic content (comma-separated)
# Masks personal/dynamic data in screenshots for privacy
MASK_SELECTORS=.user-name,.timestamp,.avatar,.user-menu,.workspace-name,.profile-photo

# LLM Configuration
LLM_MODEL=gpt-4-turbo-preview
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=4000

# Crawler Configuration
CRAWL_TIMEOUT=120000
VIEWPORT_WIDTH=1920
VIEWPORT_HEIGHT=1080
HEADLESS=false

# Pages to crawl (comma-separated paths)
# Agent will auto-discover actual URLs after login
PAGES_TO_CRAWL=/app/home,/app/projects,/app/tasks
